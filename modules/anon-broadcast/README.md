# AnonBroadcast (loopback-only)

Style tool for a local communique receipt. Radios stay off.
`publish()` is refused (`QNM-ANON-NO-PUBLISH`). This is never a
publish path and is not a Softwares-tab product.

Spec: QNM-BUILD-1.0. Pair-bind: AIH-WP-1.3 (never a publish path).
Author: Aziel Eliab only.
